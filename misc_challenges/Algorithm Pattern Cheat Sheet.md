Algorithm Pattern Cheat Sheet

1. Sliding Window
   Trigger words:

“Longest” or “maximum” subarray/substring
“At most k” or “exactly k”
“Continuous” or “consecutive” elements

Examples:

Longest substring with at most k distinct characters
Max consecutive 1s with at most k flips
Minimum window substring

2. Two Pointers
   Trigger words:

“Sorted array”
“Pair sum” or “triplet sum”
“Remove duplicates”
“Reverse in place”

Examples:

Two-sum in sorted array
Merge two sorted arrays
Remove duplicates from sorted array

3. Prefix Sum / Hash Map
   Trigger words:

“Subarray sum equals k”
“Count subarrays”
“Balance” or “difference”

Examples:

Subarray sum equals k
Longest subarray with equal 0s and 1s

4. Binary Search
   Trigger words:

“Find” or “search” in sorted data
“Minimum” or “maximum” value satisfying condition
“Rotated array”

Examples:

Search in rotated sorted array
Find first/last occurrence
Minimum capacity to ship packages

5. Dynamic Programming
   Trigger words:

“Number of ways”
“Minimum cost” or “maximum profit”
“Optimal strategy”
“Overlapping subproblems”

Examples:

Climbing stairs
Longest increasing subsequence
Knapsack problem

6. Greedy
   Trigger words:

“Minimum number of intervals”
“Earliest finish time”
“Choose best option at each step”

Examples:

Activity selection
Minimum number of arrows to burst balloons

7. Graph / BFS / DFS
   Trigger words:

“Connected components”
“Shortest path”
“Traversal”
“Island” or “grid”

Examples:

Number of islands
Shortest path in a grid
Detect cycle in graph

💡 Pro tip: When you see “longest” + “at most k” → Sliding Window.
When you see “sorted” → Two Pointers or Binary Search.
When you see “number of ways” → Dynamic Programming.
